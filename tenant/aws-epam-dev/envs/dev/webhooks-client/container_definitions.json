{
  "name"      : "${container_name}",
  "image"     : "${image}",
  "essential" : true,
  "cpu": 256,
  "memory": 512,
  "port_mappings" : [
    {
      "name": "${container_name}-${container_port}",
      "containerPort": 80,
      "hostPort": 0,
      "protocol": "tcp"
    }
  ],
  "environment" : [
    {"name": "ASPNETCORE_URLS", "value": "http://0.0.0.0:80"},
    {"name": "Token", "value": "6168DB8D-DC58-4094-AF24-483278923590"},
    {"name": "IdentityUrl", "value": "${identity_url}"},
    {"name": "CallBackUrl", "value": "${callback_url}"},
    {"name": "WebhooksUrl", "value": "${webhooks_url}"},
    {"name": "SelfUrl", "value": "${self_url}"}
  ],
  "readonly_root_filesystem": false,
  "log_configuration" : {
    "logDriver" : "awslogs",
    "options" : {
        "awslogs-region" : "${region}",
        "awslogs-group" : "${cloudwatch_log_group_name}",
        "awslogs-stream-prefix" : "ecs"
    }
  }
}
